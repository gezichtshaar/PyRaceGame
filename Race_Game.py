from Entity import Entity
from Car import Car

class Race_Game(object):
    def __init__(self):
        self.running = False
        self.paused = False
        self.entities = []

    def run(self):
        #Run all the shizzle here
        entities.append(Car(self, [0, 0], 0, 0))
