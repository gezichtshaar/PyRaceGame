import pygame

class Content(object):
    def __init__(self):
        self.load_sprites()
        self.load_sound()
        self.load_map()

    def load_sprites(self):
        #load all sprites
        foo = "bar"

    def load_sound(self):
        #load all sound files
        foo = "bar"

    def load_map(self):
        #load map here
        foo = "bar"
